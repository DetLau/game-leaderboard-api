# Node modules directory
node_modules/

# Environment files (ensure sensitive info stays local)
.env
mongo.env

# Service account keys (if applicable)
serviceAccountKey.json

# Build output directory
dist/

# macOS system files
.DS_Store
