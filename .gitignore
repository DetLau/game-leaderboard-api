# Node.js dependencies
/node_modules/

# Environment variables and configuration files (do not commit these as they contain secrets)
.env
.env.local
mongo.env

# Firebase service account key (do not commit this secret)
serviceAccountKey.json

# Build and output directories
/dist/
/build/

# System files (macOS, Windows, etc.)
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Temporary files and editor backups
*.swp
*.swo
*~
